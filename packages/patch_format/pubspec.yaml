name: hyfens_patch_format
description: Canonical Hyfens Patch Format v1 and release baseline manifests.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  crypto: ^3.0.6

dev_dependencies:
  test: ^1.26.0
