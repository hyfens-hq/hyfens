# Task 220: Clean dashboard routes

Status: [x] Completed

## Goal

Remove temporary audit query parameters and hash fragments from dashboard URLs while preserving section navigation, deep links, and browser back/forward behavior.

## Scope and Non-goals

Scope:

- Replace internal `#view` navigation with clean root paths such as `/applications` and `/settings`.
- Canonicalize legacy hash/query URLs to clean paths on dashboard startup.
- Preserve active-page rendering and browser history navigation.
- Make the local static server and Docker/Nginx deployment serve clean dashboard paths.
- Add focused route contract and live browser checks.

Non-goals:

- No change to API endpoint URLs, auth/device callback query contracts, or control-plane routes.
- No redesign of the dashboard navigation or changes to page content.
- No retention of the temporary `audit` query parameter in product behavior.

## Owner

Coordinator: Codex. Implementation and review are single-threaded because routing, static fallback, and the related tests form one small dependency-bound package.

## Dependencies

- Existing view map and sidebar links in `dashboard/app.js` and `dashboard/index.html`.
- Existing static fallback in `dashboard/nginx.conf`.
- Local Python static/proxy server and tests in `dashboard/serve.py` and `dashboard/test_serve.py`.
- Local dashboard Docker service on `127.0.0.1:18083`.

## Assumptions

- The dashboard is hosted at the root of its origin, so clean view paths are `/`, `/applications`, `/environments`, `/releases`, `/patches`, `/artifacts`, `/deployments`, `/audit`, and `/settings`.
- The current `?audit=219` query was a coordinator validation URL and is not generated by application code; startup canonicalization will also remove it if a stale validation URL is revisited.
- Legacy hash links should be accepted once and immediately replaced with the corresponding clean path.

## Work Items

- [x] Add failing tests for clean navigation, legacy URL canonicalization, and static route refresh.
- [x] Replace hash-based view updates with History API navigation and clean link targets.
- [x] Add clean-route fallback to the local Python static server and update dashboard documentation/contracts.
- [x] Rebuild Docker and verify navigation plus back/forward behavior in the live browser.
- [x] Record validation and close the task.

## Validation

Completed:

- The clean-route and static-refresh regressions failed before implementation and passed after implementation.
- `python3 -m unittest dashboard.test_serve` — 20 tests passed.
- `node --check dashboard/app.js` — passed.
- `sh -n scripts/local-dashboard.sh` — passed.
- Docker Compose config validation — passed.
- Rebuilt and recreated the canonical dashboard service with `app.js?v=220`.
- Browser verification confirmed `?audit=219#overview` canonicalizes to `/`, section changes use clean paths, and back/forward restores the active view.
- Direct Docker navigation to `/applications` rendered the correct page without a fragment or query.

## Next Action

No further action is required within this task.

## Blockers

None currently.

## Outcome

Dashboard navigation now uses clean root paths and the History API. Temporary audit queries and legacy hash fragments are removed on startup, while clean paths remain refreshable in both Docker/Nginx and the local Python server.

## References

- User-provided live URL showing `?audit=219#overview`.
- `dashboard/index.html`
- `dashboard/app.js`
- `dashboard/serve.py`
- `dashboard/nginx.conf`
- `dashboard/test_serve.py`

Validation evidence:

- `dashboard/test_serve.py` — 20 tests passed.
- `node --check dashboard/app.js` — passed.
- `sh -n scripts/local-dashboard.sh` and Docker Compose config validation — passed.
- Local Docker/browser verification passed on `127.0.0.1:18083`.

## History

- 2026-09-01: Reserved after browser reproduction showed sidebar navigation updating the address to `#applications`; the `audit=219` query was confirmed to be a temporary validation URL.
- 2026-09-01: Added clean History API routes, legacy URL canonicalization, static refresh fallback, cache-busted app code, and live navigation/back-forward verification.
