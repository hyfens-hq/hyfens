name: instrumentation_e0
description: Throwaway E0 source-overlay dispatch experiment.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  analyzer: ^8.0.0
  crypto: ^3.0.6
  package_config: ^2.2.0

dev_dependencies:
  lints: ^6.0.0
  pure_dep:
    path: fixture/pure_dart_dependency
  test: ^1.26.0
